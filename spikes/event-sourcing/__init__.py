"""Event-sourcing spike package (AC-35.1, Phase H).

Decision artifact — not production code (AC-35.1.d).

Note: the importable Python package is ``spikes.event_sourcing`` (underscore).
This directory (with a hyphen) holds the test file and the source replica.
"""
